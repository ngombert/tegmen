# OpenRouter API Key (required)
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# OpenAI API Key (optional, for real models)
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic API Key (optional, for real models)
ANTHROPIC_API_KEY=your-anthropic-api-key-here


# Database Configuration (per agent, auto-configured in Docker)
DATABASE_URL=postgresql+asyncpg://tegmen:tegmen@localhost:5432/tegmen

# LLM Model (optional, defaults to gemini-2.0-flash)
DEFAULT_MODEL=openrouter/google/gemini-2.0-flash-001

# Embedding Model for Semantic Router (optional)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Microservices Mode (true = use A2A, false = local agents)
MICROSERVICES_MODE=false

# Debug mode (optional)
DEBUG=false

# Agent Gourmet - Resilience Configuration
# Timeout (ms) for any persistence operation (default: 3000ms)
GOURMET_PERSISTENCE_TIMEOUT_MS=3000

# A2A Communication Timeout (seconds)
DEFAULT_A2A_TIMEOUT=30.0

# LLM Timeouts (ms)
ACADOMIE_LLM_TIMEOUT_MS=30000
GOURMET_LLM_TIMEOUT_MS=30000

# Artificial delay (ms) injected before persistence for chaos testing (default: 0 = disabled)
GOURMET_ARTIFICIAL_DELAY_MS=0
